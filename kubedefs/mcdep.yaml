apiVersion: apps/v1
kind: Deployment
metadata:
  name: vpromc
  labels:
    app: vpromc
    tier: cache
spec:
  selector:
    matchLabels:
      app: vpromc
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: vpromc
    spec:
      containers:
      - name: vpromc
        image: memcached
        ports:
        - name: vpromc-port
          containerPort: 11211
